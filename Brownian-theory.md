# sin も cos も微分方程式も要らない
## 足し算だけでブラウン運動を再現してみた

---

:::message
このシミュレーションには、三角関数も微分方程式も確率密度関数も登場しません。
使うのは**整数の足し算だけ**です。
:::

---

## はじめに

花粉粒子が水の中でランダムに動き続ける。
1827年にロバート・ブラウンが観察したこの現象を、物理学はこう記述します：

```
dx/dt = ξ(t)  （ランダム力の微分方程式）
⟨x²⟩ = 2Dt   （拡散方程式）
```

**本当にこれが必要でしょうか？**

24方向の整数テーブルと足し算だけで、同じ現象が再現できます。
エントロピーも増大します。渦も生まれます。花粉粒子も漂います。

「一体何が言いたいんだ」と思った方、それがシメシメです。
読み進めてください。

---

## なぜ足し算だけで十分なのか

### ランダムから秩序が生まれる瞬間

ブラウン運動には、離散加算理論の核心が凝縮されています。

1. **離散性** — 分子の衝突は量子化されたイベント
2. **加算の連鎖** — 無数の小さな力が足し合わさる
3. **エントロピー増大** — 秩序から無秩序へ、しかし局所的な秩序も生まれる
4. **φへの収束** — 長時間では拡散パターンが普遍的な形を取る

これらすべてが、三角関数も微分も使わず、**24方向離散周期の離散方向と足し算だけ**で再現できます。

---

## 24方向離散周期 — 方向を整数で表す

通常のシミュレーションでは `sin(θ)`, `cos(θ)` を使って方向ベクトルを計算します。

離散加算理論ではこれを24方向の整数テーブルに置き換えます：

```python
PISANO_24_DIRS = [
    (10, 0),   # 方向0: 右
    (10, 3),   # 方向1
    (9, 5),    # 方向2
    (8, 7),    # 方向3
    ...
    (0, -10),  # 方向19: 下
    ...
]
```

**これが核心です。**

`sin(15°) = 0.2588...` という無限小数に依存する代わりに、`(10, 3)` という整数ペアで近似します。誤差は発生しますが、離散加算理論の主張によればその誤差こそが「物理的なうねり」として機能します。

24方向は 離散周期 周期と対応しています。フィボナッチ数列を24で割った余りは周期24を持ち、この周期性が方向シフトの「波数空間の離散基底」として機能します。

---

## 実装：3つの足し算の連鎖

シミュレーションは3つのステップで構成されます。いずれも足し算（と、その後の正規化）だけです。

### ステップ1：熱運動（ランダムキック）

```python
def apply_thermal_motion(self):
    for y in range(self.height):
        for x in range(self.width):
            # 24方向離散周期からランダムに方向を選ぶ
            random_dir = np.random.randint(0, 24)
            dx, dy = PISANO_24_DIRS[random_dir]

            # 温度に応じた強さで力を加える（足し算）
            kick_strength = self.temperature
            self.vx[y, x] += (dx * kick_strength) // 15
            self.vy[y, x] += (dy * kick_strength) // 15
```

`sin/cos` なし。ランダムに選んだ整数インデックスで方向を決め、足し算で速度に加えるだけです。

これが「水分子の衝突」に相当します。

### ステップ2：拡散（密度の分配）

```python
# 密度を8方向に均等に分ける（足し算）
share_per_neighbor = current_density // 10  # 10%ずつ分配
remaining = current_density - share_per_neighbor * 8

for dy_offset in [-1, 0, 1]:
    for dx_offset in [-1, 0, 1]:
        new_density[ny, nx] += share_per_neighbor
```

`∇²ρ = D・∂ρ/∂t`（拡散方程式）の代わりに、「10分の1ずつ隣に渡す」という足し算で拡散を表現します。

### ステップ3：離散周期方向シフト（波数成分の発生）

:::message alert
**注：** 基本版では方向判定に `np.arctan2` を使用していますが、これは「三角関数を使わない」という主張と矛盾します。より厳密な実装として **`brownian_integer_only.py`** では、`arctan2` も使わず完全整数演算のみで方向を決定しています。
:::

**基本版（arctan2使用）：**

```python
# 現在の速度を24方向に離散化
angle_index = int((np.arctan2(current_vy, current_vx) * 24 / (2 * np.pi)) % 24)

# 24方向離散周期でシフト
pisano_shift = (angle_index + self.step) % 24
dir_dx, dir_dy = PISANO_24_DIRS[pisano_shift]

# 新しい速度（足し算 + 摩擦）
new_vx[y, x] = (current_vx * 6 + avg_vx * 3 + dir_dx / 10) / 9
```

**完全整数演算版（arctan2不使用）：**

```python
def get_direction_index_integer_only(vx, vy):
    """整数演算のみで24方向を決定"""
    # 符号で4象限を判定
    sign_x = 1 if vx >= 0 else -1
    sign_y = 1 if vy >= 0 else -1
    
    # 比率を整数で計算（tan(θ) ≈ vy/vx）
    abs_vx, abs_vy = abs(vx), abs(vy)
    if abs_vx > 0.1:
        ratio = int((abs_vy * 100) / abs_vx)  # 100倍して整数化
    else:
        ratio = 1000  # ほぼ垂直
    
    # 比率の範囲で24方向のどれかを決定（足し算・比較のみ）
    if sign_x > 0 and sign_y >= 0:  # 第1象限
        if ratio < 30:    idx = 0   # tan < 0.3
        elif ratio < 60:  idx = 1   # tan < 0.6
        elif ratio < 100: idx = 2   # tan < 1.0
        # ... 以下続く
    # ... 他の象限も同様
    
    return idx
```

この方法では：
1. vx, vy の符号から4象限を判定（if文のみ）
2. 比率 `vy/vx` を整数で計算（掛け算・割り算）
3. 比率の範囲で24方向を決定（比較演算のみ）

**sin, cos, arctan2, πなど、三角関数は一切使っていません。**

各ステップで方向インデックスが `self.step` だけシフトします。これにより、ランダムな揺らぎが特定の方向へと「整列」され、渦のような局所的な秩序が自然に生まれます。

---

## 繋がった2点（異物）の実装

離散加算理論シミュレーションの真骨頂は、「異物が熱運動の中で漂う」様子にあります。

河に竿を出したとき、竿先の2点が流れに揺られながら漂うイメージです。

```python
class LightLinkedPair:
    """軽く漂う繋がった2点"""
    def __init__(self, x1, y1, x2, y2, mass=8):
        self.x1, self.y1 = float(x1), float(y1)
        self.x2, self.y2 = float(x2), float(y2)
        self.vx, self.vy = 0.0, 0.0
        self.angular_velocity = 0.0
        self.mass = mass  # 軽いほど流れに乗りやすい

    def apply_force(self, fx, fy):
        """周囲の流体から力を受ける（足し算）"""
        self.vx += fx / self.mass
        self.vy += fy / self.mass

    def apply_torque(self, torque):
        """熱揺らぎによるランダムな回転（足し算）"""
        self.angular_velocity += torque / (self.mass * 1.5)
```

2点の特徴：

- **質量8**（背景流体より軽い）→ 流れに乗りやすい
- **減衰0.98**（摩擦が小さい）→ 慣性で漂い続ける
- **双方向結合** → 異物が流体を動かし、流体が異物を動かす

### 双方向結合の実装

```python
# 流体 → 異物（異物が流れに押される）
self.linked_pair.apply_force(fx / 5, fy / 5)

# 異物 → 流体（異物が周囲に影響を与える）
for px, py in [(x1, y1), (x2, y2)]:
    for dy in range(-1, 2):
        for dx in range(-1, 2):
            self.vx[ny, nx] += self.linked_pair.vx / 12
            self.vy[ny, nx] += self.linked_pair.vy / 12
```

これがブラウン運動の本質的な非対称性です。花粉粒子（異物）は水分子（流体）に押されながら、同時に水分子を動かしています。足し算だけで、この相互作用が表現されています。

---

## エントロピー増大の観測

Shannon エントロピーで拡散の進行度を定量化できます：

```python
# エントロピー計算（密度分布の広がり具合）
density_normalized = sim.density / np.sum(sim.density)
density_nonzero = density_normalized[density_normalized > 1e-10]
entropy = -np.sum(density_nonzero * np.log(density_nonzero + 1e-10))
```

実行結果：

```
初期エントロピー ≈ 0.0  （中央に密度集中）
最終エントロピー ≈ 4.2〜4.5  （全体に拡散）
増加率 ≈ 0.028〜0.03 / step
```

**これは熱力学第二法則の再現です。**

微分方程式も確率論も使わず、24方向離散周期のランダム方向選択と足し算の連鎖だけで、エントロピーが増大します。

---

## 離散加算理論的解釈

このシミュレーションで起きていることを、離散加算理論の言葉で表すと：

| 物理現象 | 離散加算理論の実装 |
|---|---|
| 熱運動（分子の衝突） | 24方向離散周期からのランダム方向キック（足し算）|
| 拡散 | 密度の近傍分配（足し算 + 正規化）|
| ブラウン粒子の運動 | 軽い異物への力の累積（足し算）|
| エントロピー増大 | 密度分布の散らばり（自然に発生）|
| 渦・局所秩序 | 離散周期方向シフトによる整列（足し算の副産物）|

**三角関数も微分方程式も確率密度関数も、何一つ使っていません。**

それでも、ブラウン運動の本質的な挙動が再現されます。

---

## 実行方法

```bash
# 基本的なブラウン運動
python brownian_motion.py

# 繋がった2点（異物）版
python brownian_linked_pair.py

# 漂う感じを強調した版
python brownian_drifting_v3.py
```

**依存ライブラリ：**

```bash
pip install numpy matplotlib
```

**パラメータ調整：**

```python
# 温度（熱運動の強さ）
temperature = 7  # 大きいほど激しく揺れる

# 異物の質量（軽いほど流れに乗りやすい）
mass = 8  # 小さいほどよく漂う

# 摩擦（小さいほど慣性が残る）
self.vx *= 0.98  # 1.0に近いほどふわふわ漂う
```

---

## 観察できる現象

### 1. ランダムウォーク

異物（繋がった2点）の軌跡は、ジグザグとしたランダムウォークを描きます。これは花粉粒子の実際のブラウン運動と同じパターンです。

### 2. 局所的な渦

24方向離散周期の方向シフトにより、密集した場所で矢印が局所的に回転する瞬間が生まれます。ランダムから秩序が生まれる瞬間です。

### 3. 異物の回転

繋がった2点は、並進運動と同時に回転します。熱揺らぎによるランダムなトルクが角速度として蓄積されるためです。

### 4. エントロピーの上昇曲線

最終状態の PNG では、エントロピーの時間発展がきれいな上昇曲線を描いています。足し算の連鎖から、熱力学第二法則が自然に現れます。

---

## まとめ

ブラウン運動は、離散加算理論が最初にカチリとつながった現象です。

それは偶然ではありません。ブラウン運動の本質は：

1. **離散的なイベントの積み重ね**（衝突 = 足し算）
2. **ランダム性の中の秩序**（24方向離散周期の周期性）
3. **双方向の相互作用**（異物 ⇔ 流体）

これらはすべて、離散加算理論の計算原理そのものです。

> *"The universe computes with addition alone."*
>
> 宇宙は足し算だけで計算している。
> ブラウン運動は、その最初の証拠です。

---

## 関連ドキュメント

- [離散加算理論 基礎理論](./theory.md)
- [河の流れシミュレーション](./river_simulation.md)
- [24方向離散周期と波数空間](./pisano24_wavespace.md)

---

## 変更履歴

- 2026-02-17: 初版作成（Claude × Google AI 共同作業）

---

**END OF DOCUMENT**
